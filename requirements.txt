# Transcription — Deepgram Nova-3 cloud API (replaces local WhisperX)
deepgram-sdk>=3.0.0

# Audio cleanup — AI speech denoising (DeepFilterNet3, GPU-accelerated)
# Install separately: see PROJECT.md for PyTorch + deepfilternet install steps

# Claude API — clip selection
anthropic>=0.40.0

# FFmpeg Python bindings — video probe / duration
ffmpeg-python>=0.2.0

# Streamlit — local browser UI
streamlit>=1.40.0

# Audio I/O backend
soundfile>=0.12.0

# Environment variable loading
python-dotenv>=1.0.0

# Profanity filtering
better-profanity>=0.7.0

# Data table in review UI
pandas>=2.0.0

# MCP server — Claude Desktop Cowork integration
mcp>=1.0.0

# Social media publishing — YouTube Shorts upload
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
